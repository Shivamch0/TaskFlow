# TaskFlow Frontend

React + Vite frontend for TaskFlow.

## Scripts

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

## Environment

Create `Frontend/.env`:

```env
VITE_API_URL=http://localhost:5000
```

The app expects the backend API at `${VITE_API_URL}/api/v1`.

